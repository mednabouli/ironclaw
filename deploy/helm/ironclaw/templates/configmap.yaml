apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ include "ironclaw.fullname" . }}-config
  labels:
    {{- include "ironclaw.labels" . | nindent 4 }}
data:
  ironclaw.toml: |
    {{- toYaml .Values.config | nindent 4 }}
